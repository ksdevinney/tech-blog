<!-- starter code from class-->
{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col">
    <h4>{{post.title}}</h4>
    <div class="card">
  <div class="card-body">
    {{post.body}}
  </div>
</div>
  </div>
</div>
{{/each}}